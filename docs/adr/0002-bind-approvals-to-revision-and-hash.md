# Bind Approvals to artifact revision and hash

Every Approval identifies an Artifact, revision, and content hash rather than recording milestone consent in isolation. This prevents an Approval from silently surviving changed content while allowing the Creator to approve a reconciled revision explicitly.
